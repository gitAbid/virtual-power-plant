spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql
  r2dbc:
    pool:
      enabled: true
      initial-size: 5
      max-size: 10

logging:
  level:
    org.springframework.r2dbc: DEBUG
    com.chellenge.vpp: DEBUG
